server:
    port: 8181

spring:
    datasource:
        url: jdbc:mysql://localhost:3307/fruit?serverTimezone=UTC
        username: "root"
        password: "001314"
        driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
    configuration:
        log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    mapper-locations: classpath:com/rich/mapper/xml/*.xml

